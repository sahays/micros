# ==============================================================================
# Micros Monorepo - Shared Infrastructure Configuration
# ==============================================================================
# This file contains environment variables shared across multiple services
# and infrastructure components. Copy this file to .env before running
# docker-compose up.
# ==============================================================================

# ------------------------------------------------------------------------------
# Infrastructure Ports (Host Access)
# ------------------------------------------------------------------------------
PROMETHEUS_PORT=9090
LOKI_PORT=3100
GRAFANA_PORT=3000
PROMTAIL_PORT=9080
MONGO_PORT=27017
REDIS_PORT=6379

# ------------------------------------------------------------------------------
# Service Ports (Host Access)
# ------------------------------------------------------------------------------
AUTH_SERVICE_PORT=8080
SECURE_FRONTEND_PORT=8081

# ------------------------------------------------------------------------------
# Infrastructure Credentials & Settings
# ------------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ------------------------------------------------------------------------------
# Shared Application Configuration
# ------------------------------------------------------------------------------
MONGODB_DATABASE=auth_db
ADMIN_API_KEY=dev-admin-key
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Secure Frontend Configuration
# ------------------------------------------------------------------------------
APP_AUTH_SERVICE__CLIENT_ID=secure-frontend
APP_AUTH_SERVICE__SIGNING_SECRET=your-signing-secret
APP_SERVER__SESSION_SECRET=your-session-secret

# ------------------------------------------------------------------------------
# Auth Service Configuration
# ------------------------------------------------------------------------------
SERVICE_VERSION=0.1.0
JWT_ACCESS_TOKEN_EXPIRY_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRY_DAYS=7
JWT_APP_TOKEN_EXPIRY_MINUTES=60
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
REQUIRE_SIGNATURES=false
ENABLE_SWAGGER=public

# ------------------------------------------------------------------------------
# External Services Configuration
# ------------------------------------------------------------------------------
# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Gmail SMTP
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# ------------------------------------------------------------------------------
# Infrastructure URIs (For LOCAL host-based development)
# ------------------------------------------------------------------------------
# These are used when running services directly on the host machine.
# DO NOT use these if you want docker-compose to override them.
# MONGODB_URI=mongodb://localhost:27017
# REDIS_URL=redis://localhost:6379
