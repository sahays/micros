server:
  host: "0.0.0.0"
  port: 8080
  session_secret: "dev-session-secret-change-in-production"
auth_service:
  url: "http://localhost:9005"  # Auth service Docker port
  public_url: "http://localhost:9005"  # URL accessible from browser
document_service:
  url: "http://localhost:9007"  # Document service Docker port
  # Secret for generating time-limited signed URLs for shareable document downloads
  document_signing_secret: "dev-document-signing-secret-replace-in-production"
